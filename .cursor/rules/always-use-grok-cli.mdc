---
description: PAUSED by user — Grok CLI forced routing disabled (restore from .cursor/hooks/*.paused.bak).
alwaysApply: false
---

# Grok CLI routing — PAUSED

User instructed: Grok 点不了 / 暂定强制走 Grok CLI. Hooks cleared in `.cursor/hooks.json`.

**Restore later:**
1. `cp .cursor/hooks/hooks.json.grok-forced.bak .cursor/hooks.json`
2. `cp .cursor/hooks/always-use-grok-cli.mdc.paused.bak .cursor/rules/always-use-grok-cli.mdc`
3. Re-auth Grok on keeper VM / Environment Save when available.

Until then: use ChatGPT web PLAN/REVIEW + opencode/ds4.1 Work (B+C). Do not force `run.sh`.
