# PIXEL_VERDICT · 71ca4af vs 0de4f1d（8.6 物证）

对象：`docs/eval_71ca4af/` 新窗口 dump vs `docs/eval_0de4f1d/`（**未删、未覆写**）。  
HEAD 脚本：`scripts/eval_dump_71ca4af.gd`。读图人只看 PNG，不把 `DUMP_HUD` / 冒烟契约绿写成像素绿。  
**不报 9.5。** 本页只锁读图，不打门槛加权。

## Dump 元数据

- `DUMP_OK eval_71ca4af` **是**。`DUMP_EXIT=0`。31/31 PNG 皆 1280×720。
- `DUMP_HEAD display=X11 size=(1280, 720)`；OpenGL/llvmpipe；ALSA 失败 → Dummy。退出 `4 ObjectDB instances leaked`（与 0de4f1d 同）。
- `DUMP_YARD_FAIL ok=true reason=escape tick=965`
- `DUMP_RADIO_FAIL ok=true reason=escape route=sneak leaker=3 tick=1502`
- 参考胜 tick 与旧证一致：院子 win 283 / 仓 872 / 泵 740 / 楼 818 / 库 572 / 电台 win 730。观战 `radio_watch t=6.6 tick=399`（旧 397，2 tick，不当漂）。
- HUD 文本契约仍在（`card=漏网：侧翼…` / `漏网：暗道…`）。**下面每条以像素为准。**

---

## §7.2 读图锁门

### 1. `10_fail_yard_card` + `23_fail_radio_card` — **PASS**

新 `10`：结果牌居中，左操作员轨（灰狼/铁砧/夜枭）**已关**。标题「失败 · 逃逸」整词可读；三行「漏网：侧翼 · 敌3 · 0.4s出发 · 东廊 / 改一处就能赢：… / 带着情报穿梭回去」完整露出。东南「逃逸口·越界 / 缺口·侧翼」红剪影仍在牌右。旧 `10` 是铁砧卡把标题咬成「失败 · 逃…」、夜枭卡挡住「漏网：」。

新 `23` 同构图：青字「失败 · 逃逸」+「漏网：暗道 · 敌3 · 3.6s出发 · 西暗道」完整；左轨关；东南「缺口·暗道」仍露。桌面 ExtraBar/BottomBar 在失败牌上也关了（旧图键条压牌脚）。

牌内迷你时间轴「主0.0 / 侧0.4」叠字，见文末剩余红，**不翻这条主问。**

### 2. `13_win_yard` + `26_win_radio` — **PASS**

新 `13`：「下一关」是结果牌底下一整条可点按钮，「返回标题」在其下；ExtraBar（开火/音效/菜单）与 BottomBar 已关。旧 `13`「下一关」被「开火: 见敌即打 (F) / 音效 M / 菜单 Esc」咬成键缝残片。

新 `26`：「查看致谢」同样是整条按钮，不再挤在「开火 / 弹包 / 音效」中间。旧 `26` 正是那张打脸图。

### 3. `12_setup_yard_restored` + `24_setup_radio_ref` — **FAIL**

新 `12` 与旧 `12` 几乎同一构图：橙色长句「改一处就能赢：侧翼奔袭从东廊漏出（敌3 · 0.4s出发）。建议在 15.7 秒前加强侧翼（敌3）」横穿整条路线时间轴；「已恢复上轮计划」又切过「主 / 侧 / 第二层」芯片。时间轴主/侧/暗点仍在，但被情报条咬。

新 `24` 比旧 `24` 略干净（标题不再被裁成「BUSH LOOP」，清单收到右上），但「相对上轮：绊索0→1」仍是时间轴正中一块黑芯片，左侧「改一处就能赢：西暗道影探…」仍压住 主/侧/暗/回 点。空布置的干净不能套到第二世。

### 4. `19_setup_depot_empty` vs `22_setup_radio_empty` — **PASS**

缩略图能分开：`19` 危橙钠灯 + 院内三枚罐圆；`22` 磷光青 + 竖灯塔桅（「碟台」在桅顶、「灯塔脊」在院西）+ 碟阵 + 青线「回波·碟缝 / 回波 5.2s」。电台院落是地砖厅，不是罐区矩形。`(15,8)` 在灯塔西侧是可行走地砖（油库同坐标是罐墙）。骨架仍是西夹缝/南闸/东廊，但「灯塔+碟 vs 罐场」这一问过了。

### 5. `15_setup_warehouse_empty` / `16_setup_warehouse_hold` — **PASS**

旧 `15`/`16` 第二层写在东廊脊上：「第二层：过早开火会打空」。新两张黄虚线伏击框内写「第二层：等黄区再打（F 入伏）」，另有「伏击区 · F 入伏再打」；爆心油桶仍标在东廊，标签不在脊上。`16` 绿条「弹包给铁砧 — 这关就该这样」仍横切时间轴，见剩余红，不翻黄区这一问。

### 6. `29_journal_stamped` — **PASS**

旧 `29` 是带滚动条的文章，一屏只到第 3 夜。新 `29` 是六行图板：初阵/深仓/闸站/信号/油库/终夜全部「已封锁」，第 4–6 夜不用滚。空档 `03_journal_empty` 同一块板（院子「可出击」，后五夜「锁定」）。

### 7. `31_touch_radio_bars` — **PASS**

触控 HUD 开时桌面 ExtraBar（「开火: 见敌即打 (F)」那一排）和 BottomBar（拉响警报/收回/工具/速度/暂停）关掉，底栏只剩 开火/弹包/绊索/↻↺/收回/警报。东南逃逸口紫三角 +「逃逸口」仍在（字偏淡，南沿有一条绿遮幅，与旧 `31` 同级，口没有被键条盖住）。

### 8. `05_briefing_yard` — **PASS**（相对旧证：重复「必须带」已去掉）

旧 `05` 正文「必须带 · 铁砧扫东箱…」下面第一条子弹又是「这关必须带铁砧扫东箱侧翼…」。新 `05` 只留一行必须带，第一条子弹改成「夜巡小队从北门进院子…」。底部仍有一段键表（含「卡上写本关必须带谁」），那是操作说明不是同一句印两遍。简报仍然偏厚，不把这条标红。

### 9. `01_title` / `08_setup_yard_facing` / `25_watch_radio_echo` — **FAIL**（08、25 仍红）

- `01_title`：**PASS**。字标、副题、开始行动脉冲、继续（灰）、档案/说明/退出，与旧证同级，无新咬字。
- `08_setup_yard_facing`：**FAIL**。状态「灰狼 →「西侧掩体」弹7 · 射界朝东 · 掩体保护朝西（来袭减伤60%，侧背无减免）」整句切过「主 / 侧 / 第二层」芯片，与旧 `08` 同一刀。射界/保护两词在罗盘上仍可读（「射朝东 0°」），但顶栏不可读。
- `25_watch_radio_echo`：**FAIL**（可读性）。`t≈6.6s` 打出「灯塔回波到了 — 敌5」、铁砧碟台朝南、时间轴停在回 5.2——课成立。但同一句 callout 印了两遍：顶条黑芯片 + 地图正中大字盖住灯塔。旧 `25` 同一病。

---

## 剩余红像素（要改代码，不是再跑 dump）

1. **情报/状态/弹包条横切时间轴** — `12` `24` `08` `16`。提示：`main.gd` 停靠 / `_refresh_intel_chip` / 状态条 / 弹包绿条。空布置（`07`）干净不能当第二世过关。
2. **失败/通关牌内迷你时间轴叠字** — `10` `23` `26`：「路线时间轴」与「灰/连」菱、`主0.0` 与 `侧0.4` 挤在一起。提示：结果牌 dock，不是左轨（左轨已关）。
3. **观战回波 callout 双印** — `25`。提示：地图层 callout vs 顶栏 status，留一个。
4. **不要动的：** 失败三行文案、`(15,8)` 开/罐墙、`second_trap_route=flank`（仓道）、参考 tick、左轨在失败/通关期保持关闭。

未在本页当绿：冒烟 `SMOKE_OK_FAIL_CARD_CLEAR` / `SMOKE_OK_WIN_CTA_CLEAR` / `SMOKE_OK_INTEL_CHIP`（没在这份窗口 dump 里复跑；即便契约绿，`12`/`24` 像素仍红）。

---

## 对照 0de4f1d Top 打脸图

| 旧打脸 | 新图 | 像素 |
|---|---|---|
| 失败牌被左卡咬掉「失败 · 逃逸」「漏网：」 | `10` `23` | 过 |
| 「下一关」「查看致谢」被 ExtraBar 埋 | `13` `26` | 过 |
| 情报芯片横切时间轴 | `12` `24` | **仍红** |
| 电台远看是油库换色 | `19` vs `22` | 过（灯塔+碟 vs 罐圆） |
| 仓道第二层在脊上「站外面」 | `15` `16` | 过（黄区「等黄区再打」） |
| 档案要滚 4–6 夜 | `29` `03` | 过 |
| 触控叠桌面双条 | `31` | 过 |
| 简报「必须带」印两遍 | `05` | 过 |

**像素总判：P0 读图未锁死。** 失败牌/CTA/黄区/灯塔/档案/触控条这几张打脸图翻了；第二世情报条仍是 8.6 那一刀。再 dump 不会变，要改停靠。
